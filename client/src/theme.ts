import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    main: 'cyan',
    secondary: 'secondary',
    brightGray: '#ebecf0',
  },
};

export { theme };
