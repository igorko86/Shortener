import styled from 'styled-components';

export const DivContentPopover = styled.div`
  // max-width: 300px;
  // min-width: 60px;
  // border-radius: 4px;
  // border: 1px solid ${({ theme }) => theme.colors.silver};
  // padding: 0 4px;
  // color: ${({ theme }) => theme.colors.midnight};
`;
