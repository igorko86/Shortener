const initialState = {
  isAuth: false,
};
const authReducer = (state = initialState, action) => {
  switch (action.type) {
    case LOGIN:
    default:
      return state;
  }
};

export default authReducer;
