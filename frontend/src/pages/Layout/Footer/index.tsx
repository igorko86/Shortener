import { FC } from 'react';
import { AppFooter, AppWrapperDiv } from './styles';

const Footer: FC = () => {
  return (
    <AppFooter>
      <AppWrapperDiv>Footer</AppWrapperDiv>
    </AppFooter>
  );
};

export default Footer;
