import styled from 'styled-components';
import { Space } from 'antd';

export const CustomModalDiv = styled.div`
  position: absolute;
  top: 50%;
  transform: translate(0, -50%);
  border: 5px solid #ffff00;
  padding: 10px;
`;
